const COOKIE = "nexauren_session";
const SESSION_DAYS = 7;

function escapeHtml(value = "") {
  return String(value).replace(/[&<>"']/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" }[char]));
}

function redirect(url, extraHeaders = {}) { return new Response(null, { status: 302, headers: { Location: url, ...extraHeaders } }); }

function page(body, title = "Nexauren", description = "Nexauren — ferramentas online rápidas, modernas e fáceis de usar.", user = null, canonicalPath = "/") {
  const authMenu = user
    ? `<a href="/account" class="nav-account">Minha conta</a><form method="post" action="/logout" class="nav-logout"><button type="submit">Sair</button></form>`
    : `<a href="/login">Entrar</a><a href="/register" class="nav-cta">Criar conta</a>`;
  const footerAccount = user
    ? `<a href="/account">Minha conta</a><form method="post" action="/logout" class="footer-logout"><button type="submit">Sair da conta</button></form>`
    : `<a href="/login">Entrar</a><a href="/register">Criar conta</a>`;
  return new Response(`<!doctype html><html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="description" content="${escapeHtml(description)}"><meta name="robots" content="index,follow"><meta name="theme-color" content="#ffffff"><link rel="canonical" href="https://nexaurenstory.com${escapeHtml(canonicalPath)}"><title>${escapeHtml(title)} | Nexauren</title><style>
:root{font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color:#172033;background:#fff;--blue:#2563eb;--violet:#7c3aed;--pink:#ec4899;--line:#e7ebf3;--soft:#f7f9fc;--muted:#667085}*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;min-height:100vh;background:#fff;background-image:radial-gradient(circle at 5% 5%,#dbeafe 0,transparent 23%),radial-gradient(circle at 95% 12%,#f3e8ff 0,transparent 23%),radial-gradient(circle at 50% 100%,#ecfeff 0,transparent 28%)}a{color:inherit;text-decoration:none}.nav{display:flex;align-items:center;justify-content:space-between;gap:20px;padding:13px max(20px,5vw);position:sticky;top:0;z-index:50;background:rgba(255,255,255,.92);backdrop-filter:blur(20px);border-bottom:1px solid var(--line);box-shadow:0 5px 25px #10182808}.brand{display:flex;align-items:center;gap:10px;font-weight:950;letter-spacing:.07em;white-space:nowrap}.brand-mark{width:36px;height:36px;border-radius:12px;background:linear-gradient(135deg,var(--blue),var(--violet),var(--pink));box-shadow:0 8px 22px #7c3aed33;position:relative}.brand-mark:after{content:"";position:absolute;inset:8px;border-radius:7px;background:#fff2}.links{display:flex;align-items:center;justify-content:flex-end;gap:5px;flex-wrap:wrap}.links a,.nav-logout button{border:0;background:transparent;padding:10px 13px;border-radius:11px;color:#475467;font:inherit;font-weight:700;cursor:pointer;transition:.2s}.links a:hover,.nav-logout button:hover{background:#f2f5fb;color:#172033;transform:translateY(-1px)}.links .nav-cta{color:#fff;background:linear-gradient(135deg,var(--blue),var(--violet));box-shadow:0 8px 20px #2563eb30}.nav-account{font-weight:850!important}.nav-logout{margin:0}.wrap{width:min(1180px,92vw);margin:0 auto;padding:42px 0 70px}.hero{position:relative;overflow:hidden;padding:72px 0 82px}.hero:after{content:"";position:absolute;width:210px;height:210px;right:4%;top:15px;border-radius:50%;background:linear-gradient(135deg,#dbeafe,#f5d0fe);filter:blur(2px);opacity:.8;z-index:-1}.eyebrow{display:inline-flex;padding:8px 13px;border-radius:999px;background:#eff6ff;color:#2563eb;border:1px solid #dbeafe;font-size:13px;font-weight:800}.hero h1{max-width:850px;font-size:clamp(44px,7vw,80px);line-height:.98;letter-spacing:-.055em;margin:18px 0}.gradient{background:linear-gradient(90deg,var(--blue),var(--violet),var(--pink));-webkit-background-clip:text;background-clip:text;color:transparent}.lead{max-width:720px;font-size:19px;line-height:1.7;color:var(--muted)}.actions{display:flex;gap:10px;flex-wrap:wrap}.btn{display:inline-flex;align-items:center;justify-content:center;border:0;border-radius:13px;padding:13px 19px;background:linear-gradient(135deg,var(--blue),var(--violet));color:#fff;font-weight:800;cursor:pointer;box-shadow:0 10px 24px #2563eb2e}.btn.secondary{background:#fff;color:#344054;border:1px solid #dfe5ef;box-shadow:0 8px 20px #1018280b}.section-head{display:flex;justify-content:space-between;align-items:end;margin:8px 0 20px}.section-head h1,.section-head h2{margin:0;letter-spacing:-.035em}.section-head h2{font-size:30px}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(230px,1fr));gap:18px}.card{border:1px solid var(--line);background:rgba(255,255,255,.94);padding:24px;border-radius:22px;box-shadow:0 16px 45px #1d4ed80d;transition:.2s}.card:hover{transform:translateY(-4px);box-shadow:0 24px 60px #1d4ed81a;border-color:#ccd7e8}.card h3{margin:12px 0 8px;font-size:20px}.muted{color:var(--muted);line-height:1.65}.pill{display:inline-flex;padding:7px 11px;border-radius:999px;background:#f4f6fb;color:#475467;font-size:12px;font-weight:750;border:1px solid #e7ebf2}.form{max-width:620px;margin:25px auto}.field{margin:16px 0}.field label{display:block;margin-bottom:8px;color:#344054;font-weight:700}.field input,.field textarea{width:100%;padding:13px 14px;border-radius:12px;border:1px solid #dfe5ef;background:#fff;color:#172033;outline:none}.field input:focus{border-color:var(--violet);box-shadow:0 0 0 4px #7c3aed14}.footer{margin-top:30px!important;padding-top:48px!important;border-top:1px solid var(--line);color:#667085;background:linear-gradient(180deg,transparent,#f8faff)}.footer-grid{display:grid;grid-template-columns:2fr 1fr 1fr;gap:35px}.footer-brand{display:flex;align-items:center;gap:10px;color:#172033;font-weight:950;letter-spacing:.07em}.footer-mark{width:32px;height:32px;border-radius:10px;background:linear-gradient(135deg,var(--blue),var(--violet),var(--pink));box-shadow:0 7px 18px #7c3aed2e}.footer h3{color:#172033;margin:0 0 14px;font-size:15px}.footer a{display:block;margin:10px 0;color:#667085;transition:.2s}.footer a:hover{color:#2563eb;transform:translateX(2px)}.footer-logout{margin:10px 0}.footer-logout button{border:0;background:none;padding:0;color:#667085;font:inherit;cursor:pointer}.footer-logout button:hover{color:#2563eb}.footer-description{max-width:360px}.footer-bottom{margin-top:38px;padding-top:20px;border-top:1px solid var(--line);font-size:13px;display:flex;justify-content:space-between;gap:15px;flex-wrap:wrap}.footer-badge{display:inline-flex;margin-top:12px;padding:7px 10px;border-radius:999px;background:#fff;border:1px solid #e7ebf3;color:#475467;font-size:12px;font-weight:700}@media(max-width:760px){.nav{align-items:flex-start;padding:11px 4vw}.links{gap:2px}.links a,.nav-logout button{padding:8px;font-size:13px}.brand{font-size:14px}.brand-mark{width:33px;height:33px}.hero{padding:50px 0}.hero h1{font-size:48px}.footer-grid{grid-template-columns:1fr;gap:28px}.section-head{display:block}}
</style></head><body><nav class="nav"><a class="brand" href="/"><span class="brand-mark"></span><span>NEXAUREN</span></a><div class="links"><a href="/">Início</a><a href="/tools">Ferramentas</a>${authMenu}</div></nav>${body}<footer class="wrap footer"><div class="footer-grid"><div><div class="footer-brand"><span class="footer-mark"></span><span>NEXAUREN</span></div><p class="muted footer-description">Ferramentas digitais modernas, rápidas e simples para transformar tarefas complexas em experiências fáceis.</p><span class="footer-badge">✦ Construído para simplificar</span></div><div><h3>EXPLORAR</h3><a href="/">Início</a><a href="/tools">Todas as ferramentas</a></div><div><h3>CONTA</h3>${footerAccount}</div></div><div class="footer-bottom"><span>© ${new Date().getFullYear()} Nexauren. Todos os direitos reservados.</span><span>Rápido · Simples · Inteligente</span></div></footer></body></html>`, { headers: { "Content-Type": "text/html; charset=UTF-8" } });
}

async function hashPassword(password, salt) { const data = new TextEncoder().encode(password + salt); const digest = await crypto.subtle.digest("SHA-256", data); return [...new Uint8Array(digest)].map((x) => x.toString(16).padStart(2, "0")).join(""); }
function randomToken() { const bytes = new Uint8Array(32); crypto.getRandomValues(bytes); return [...bytes].map((x) => x.toString(16).padStart(2, "0")).join(""); }
async function userFromRequest(request, env) { const cookie = request.headers.get("Cookie") || ""; const match = cookie.match(new RegExp(`${COOKIE}=([^;]+)`)); if (!match) return null; return env.DB.prepare("SELECT u.id,u.email,u.name,u.role FROM sessions s JOIN users u ON u.id=s.user_id WHERE s.token=? AND s.expires_at>? LIMIT 1").bind(match[1], Date.now()).first(); }

async function home(request, env) { const user = await userFromRequest(request, env); const categories = await env.DB.prepare(`SELECT c.slug,c.name,c.description,c.icon,COUNT(t.slug) AS count FROM categories c LEFT JOIN tools t ON t.category=c.slug AND t.published=1 GROUP BY c.slug,c.name,c.description,c.icon ORDER BY c.name`).all(); const featured = await env.DB.prepare("SELECT slug,name,description,category FROM tools WHERE published=1 ORDER BY created_at DESC LIMIT 12").all(); return page(`<main class="wrap"><section class="hero"><span class="eyebrow">✦ Nexauren Tools</span><h1>Ferramentas digitais <span class="gradient">feitas para simplificar.</span></h1><p class="lead">Um espaço moderno para encontrar ferramentas online rápidas, organizadas e fáceis de usar. Explore por categoria e comece em poucos segundos.</p><div class="actions"><a class="btn" href="/tools">Explorar ferramentas →</a>${user ? `<a class="btn secondary" href="/account">Minha conta</a>` : `<a class="btn secondary" href="/register">Criar conta</a>`}</div></section><div class="section-head"><div><h2>Categorias</h2><p class="muted">Encontre rapidamente o que precisa.</p></div></div><div class="grid">${categories.results.map((c) => `<a class="card" href="/tools?category=${encodeURIComponent(c.slug)}"><span class="pill">${escapeHtml(c.icon || c.slug)}</span><h3>${escapeHtml(c.name)}</h3><p class="muted">${escapeHtml(c.description || "Ferramentas Nexauren")} · ${c.count} disponível(eis)</p></a>`).join("")}</div><div class="section-head" style="margin-top:56px"><div><h2>Ferramentas recentes</h2><p class="muted">Novidades publicadas no Nexauren.</p></div></div><div class="grid">${featured.results.map((t) => `<a class="card" href="/tool/${encodeURIComponent(t.slug)}"><span class="pill">${escapeHtml(t.category)}</span><h3>${escapeHtml(t.name)}</h3><p class="muted">${escapeHtml(t.description || "Ferramenta online Nexauren")}</p><span class="gradient" style="font-weight:800">Usar ferramenta →</span></a>`).join("") || `<div class="card"><p class="muted">As primeiras ferramentas estão a ser preparadas.</p></div>`}</div></main>`, "Nexauren — Ferramentas Online", "Nexauren oferece ferramentas online rápidas, modernas e fáceis de usar para produtividade, áudio, imagem, PDF, texto e muito mais.", user, "/"); }

async function tools(request, env) { const user = await userFromRequest(request, env); const url = new URL(request.url); const category = url.searchParams.get("category"); const result = category ? await env.DB.prepare("SELECT slug,name,description,category FROM tools WHERE published=1 AND category=? ORDER BY name").bind(category).all() : await env.DB.prepare("SELECT slug,name,description,category FROM tools WHERE published=1 ORDER BY created_at DESC").all(); const canonical = category ? `/tools?category=${encodeURIComponent(category)}` : "/tools"; return page(`<main class="wrap"><div class="section-head"><div><span class="eyebrow">Nexauren Tools</span><h1>Ferramentas</h1><p class="muted">${category ? `Categoria: ${escapeHtml(category)}` : "Todas as ferramentas disponíveis."}</p></div></div><div class="grid">${result.results.map((t) => `<a class="card" href="/tool/${encodeURIComponent(t.slug)}"><span class="pill">${escapeHtml(t.category)}</span><h3>${escapeHtml(t.name)}</h3><p class="muted">${escapeHtml(t.description || "Ferramenta online Nexauren")}</p><span class="gradient" style="font-weight:800">Usar ferramenta →</span></a>`).join("") || `<div class="card"><p class="muted">Nenhuma ferramenta publicada nesta categoria.</p></div>`}</div></main>`, "Ferramentas Online | Nexauren", "Explore as ferramentas online do Nexauren por categoria.", user, canonical); }

function validToolPath(value) { return /^tools\/(audio|image|pdf|text|productivity|business|marketplace)\/[a-z0-9-]+$/.test(value); }
async function toolPage(slug, env, request) { const tool = await env.DB.prepare("SELECT slug,name,description,category,tool_path,html_file,js_file,css_file FROM tools WHERE slug=? AND published=1 LIMIT 1").bind(slug).first(); if (!tool) return new Response("Ferramenta não encontrada", { status: 404 }); if (!validToolPath(tool.tool_path)) return new Response("Caminho da ferramenta inválido", { status: 500 }); if (!env.ASSETS) return new Response("Assets não configurados", { status: 500 }); const assetUrl = new URL(`/${tool.tool_path}/${tool.html_file || "index.html"}`, request.url); const assetResponse = await env.ASSETS.fetch(new Request(assetUrl, request)); if (!assetResponse.ok) return new Response("Arquivos da ferramenta ainda não foram publicados", { status: 404 }); return assetResponse; }

function authForm(type) { const register = type === "register"; return page(`<main class="wrap"><form class="card form" method="post"><span class="eyebrow">Nexauren Account</span><h1>${register ? "Criar conta" : "Bem-vindo de volta"}</h1><p class="muted">${register ? "Crie a sua conta para aceder à experiência Nexauren." : "Entre na sua conta Nexauren."}</p>${register ? `<div class="field"><label>Nome</label><input name="name" required maxlength="80" autocomplete="name"></div>` : ""}<div class="field"><label>Email</label><input type="email" name="email" required maxlength="160" autocomplete="email"></div><div class="field"><label>Senha</label><input type="password" name="password" required minlength="8" autocomplete="${register ? "new-password" : "current-password"}"></div><button class="btn" type="submit">${register ? "Criar minha conta" : "Entrar na conta"}</button><p class="muted">${register ? `Já tem conta? <a class="gradient" style="font-weight:800" href="/login">Entrar</a>` : `Ainda não tem conta? <a class="gradient" style="font-weight:800" href="/register">Criar conta</a>`}</p></form></main>`, register ? "Criar conta | Nexauren" : "Login | Nexauren", "Aceda à sua conta Nexauren.", null, register ? "/register" : "/login"); }

async function account(request, env) { const user = await userFromRequest(request, env); if (!user) return redirect("/login"); return page(`<main class="wrap"><section class="card"><span class="eyebrow">Minha conta</span><h1>Olá, ${escapeHtml(user.name)}.</h1><p class="muted">A sua conta Nexauren está ativa.</p><div class="grid"><div class="card"><span class="pill">Perfil</span><h3>${escapeHtml(user.name)}</h3><p class="muted">${escapeHtml(user.email)}</p></div><div class="card"><span class="pill">Nexauren</span><h3>Explore as ferramentas</h3><p class="muted">Use as ferramentas disponíveis na plataforma.</p><a class="btn" href="/tools">Ver ferramentas</a></div></div></section></main>`, "Minha conta | Nexauren", "Gerencie a sua conta Nexauren.", user, "/account"); }

export default { async fetch(request, env) { const url = new URL(request.url); const path = url.pathname; const user = await userFromRequest(request, env); if ((path === "/register" || path === "/login") && user) return redirect("/account"); if (path === "/") return home(request, env); if (path === "/tools") return tools(request, env); if (path === "/account") return account(request, env); if (path === "/register" && request.method === "GET") return authForm("register"); if (path === "/login" && request.method === "GET") return authForm("login"); if (path === "/logout" && request.method === "POST") { const cookie = request.headers.get("Cookie") || ""; const match = cookie.match(new RegExp(`${COOKIE}=([^;]+)`)); if (match) await env.DB.prepare("DELETE FROM sessions WHERE token=?").bind(match[1]).run(); return redirect("/", { "Set-Cookie": `${COOKIE}=; Max-Age=0; HttpOnly; Secure; SameSite=Lax; Path=/` }); } if (path.startsWith("/tool/")) return toolPage(decodeURIComponent(path.slice(6)), env, request); if ((path === "/register" || path === "/login") && request.method === "POST") { const form = await request.formData(); const email = String(form.get("email") || "").trim().toLowerCase(); const password = String(form.get("password") || ""); const name = String(form.get("name") || "").trim(); if (!email || password.length < 8 || (path === "/register" && !name)) return new Response("Dados inválidos", { status: 400 }); if (path === "/register") { const existing = await env.DB.prepare("SELECT id FROM users WHERE email=? LIMIT 1").bind(email).first(); if (existing) return new Response("Email já registado", { status: 409 }); const id = randomToken(); const salt = randomToken(); const passwordHash = await hashPassword(password, salt); const count = await env.DB.prepare("SELECT COUNT(*) AS count FROM users").first(); const role = Number(count?.count || 0) === 0 ? "admin" : "user"; await env.DB.prepare("INSERT INTO users(id,email,name,password_hash,password_salt,role,created_at) VALUES(?,?,?,?,?,?,?)").bind(id,email,name,passwordHash,salt,role,Date.now()).run(); const token = randomToken(); await env.DB.prepare("INSERT INTO sessions(token,user_id,expires_at) VALUES(?,?,?)").bind(token,id,Date.now()+SESSION_DAYS*86400000).run(); return redirect("/account", { "Set-Cookie": `${COOKIE}=${token}; Max-Age=${SESSION_DAYS*86400}; HttpOnly; Secure; SameSite=Lax; Path=/` }); } const found = await env.DB.prepare("SELECT id,password_hash,password_salt FROM users WHERE email=? LIMIT 1").bind(email).first(); if (!found || await hashPassword(password, found.password_salt) !== found.password_hash) return new Response("Email ou senha incorretos", { status: 401 }); const token = randomToken(); await env.DB.prepare("INSERT INTO sessions(token,user_id,expires_at) VALUES(?,?,?)").bind(token,found.id,Date.now()+SESSION_DAYS*86400000).run(); return redirect("/account", { "Set-Cookie": `${COOKIE}=${token}; Max-Age=${SESSION_DAYS*86400}; HttpOnly; Secure; SameSite=Lax; Path=/` }); } return env.ASSETS?.fetch(request) || new Response("Not found", { status: 404 }); } };
