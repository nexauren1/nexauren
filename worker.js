const COOKIE = "nexauren_session";
const SESSION_DAYS = 7;

function escapeHtml(value = "") {
  return String(value).replace(/[&<>"']/g, (char) => ({
    "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;"
  })[char]);
}

function page(body, title = "Nexauren", description = "Nexauren — ferramentas online rápidas, simples e profissionais.") {
  const safeTitle = escapeHtml(title);
  const safeDescription = escapeHtml(description);
  return new Response(`<!doctype html><html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="description" content="${safeDescription}"><meta name="robots" content="index,follow"><meta name="theme-color" content="#ffffff"><link rel="canonical" href="https://nexaurenstory.com${new URL(requestUrl || "https://nexaurenstory.com/").pathname}"><title>${safeTitle} | Nexauren</title><style>
:root{font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color:#172033;background:#fff;--blue:#2563eb;--violet:#7c3aed;--cyan:#06b6d4;--pink:#ec4899;--green:#10b981;--line:#e8edf5;--soft:#f6f8fc;--muted:#667085}*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;min-height:100vh;background:#fff;background-image:radial-gradient(circle at 8% 8%,#dbeafe 0,transparent 24%),radial-gradient(circle at 92% 18%,#f3e8ff 0,transparent 24%),radial-gradient(circle at 50% 100%,#ecfeff 0,transparent 28%)}a{color:inherit;text-decoration:none}.nav{display:flex;align-items:center;justify-content:space-between;gap:18px;padding:16px max(20px,5vw);position:sticky;top:0;z-index:20;background:rgba(255,255,255,.88);backdrop-filter:blur(18px);border-bottom:1px solid var(--line)}.brand{display:flex;align-items:center;gap:10px;font-weight:950;letter-spacing:.06em}.brand-mark{width:34px;height:34px;border-radius:11px;background:linear-gradient(135deg,var(--blue),var(--violet),var(--pink));box-shadow:0 8px 22px #7c3aed33}.links{display:flex;gap:6px;align-items:center;flex-wrap:wrap}.links a{padding:9px 12px;border-radius:10px;color:#475467;font-weight:650}.links a:hover{background:var(--soft);color:#172033}.wrap{width:min(1180px,92vw);margin:0 auto;padding:42px 0 70px}.hero{position:relative;overflow:hidden;padding:72px 0 82px}.hero:after{content:"";position:absolute;width:190px;height:190px;border-radius:50%;right:4%;top:20px;background:linear-gradient(135deg,#dbeafe,#f5d0fe);filter:blur(2px);opacity:.75;z-index:-1}.eyebrow{display:inline-flex;align-items:center;gap:8px;padding:8px 13px;border-radius:999px;background:#eff6ff;color:#2563eb;border:1px solid #dbeafe;font-size:13px;font-weight:800}.hero h1{max-width:820px;font-size:clamp(42px,7vw,78px);line-height:.98;letter-spacing:-.055em;margin:18px 0}.gradient{background:linear-gradient(90deg,#2563eb,#7c3aed,#ec4899);-webkit-background-clip:text;background-clip:text;color:transparent}.lead{max-width:700px;font-size:19px;line-height:1.7;color:#667085}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(230px,1fr));gap:18px}.card{position:relative;border:1px solid var(--line);background:rgba(255,255,255,.9);padding:24px;border-radius:22px;box-shadow:0 16px 45px #1d4ed80d;transition:transform .2s,box-shadow .2s,border-color .2s}.card:hover{transform:translateY(-4px);box-shadow:0 24px 60px #1d4ed81a;border-color:#cfd9ea}.card h3{margin:12px 0 8px;font-size:20px}.muted{color:var(--muted);line-height:1.65}.pill{display:inline-flex;padding:7px 11px;border-radius:999px;background:#f4f6fb;color:#475467;font-size:12px;font-weight:750;border:1px solid #e7ebf2}.btn{display:inline-flex;align-items:center;justify-content:center;border:0;border-radius:13px;padding:13px 18px;background:linear-gradient(135deg,var(--blue),var(--violet));color:#fff;font-weight:800;cursor:pointer;box-shadow:0 10px 24px #2563eb2e}.btn:hover{transform:translateY(-1px)}.btn.secondary{background:#fff;color:#344054;border:1px solid #dfe5ef;box-shadow:0 8px 20px #1018280b}.actions{display:flex;gap:10px;flex-wrap:wrap}.section-head{display:flex;justify-content:space-between;align-items:end;gap:20px;margin:10px 0 20px}.section-head h2{margin:0;font-size:30px;letter-spacing:-.03em}.form{max-width:620px;margin:20px auto}.field{margin:16px 0}.field label{display:block;margin-bottom:8px;color:#344054;font-weight:700}.field input,.field textarea,.field select{width:100%;padding:13px 14px;border-radius:12px;border:1px solid #dfe5ef;background:#fff;color:#172033;outline:none}.field input:focus,.field textarea:focus,.field select:focus{border-color:#7c3aed;box-shadow:0 0 0 4px #7c3aed14}.field textarea{min-height:110px}.notice{padding:14px 16px;border-radius:14px;background:#f8fafc;border:1px solid var(--line);margin:14px 0}.table{width:100%;border-collapse:collapse}.table th,.table td{padding:13px;text-align:left;border-bottom:1px solid var(--line)}.status{font-size:12px;color:#059669;font-weight:800}.stats{display:grid;grid-template-columns:repeat(auto-fit,minmax(160px,1fr));gap:14px;margin:28px 0}.stat{padding:18px;border-radius:18px;background:#fff;border:1px solid var(--line);box-shadow:0 12px 30px #1018280a}.stat strong{display:block;font-size:28px}.footer{margin-top:60px;padding-top:24px;border-top:1px solid var(--line);color:#667085;font-size:14px}@media(max-width:760px){.nav{align-items:flex-start}.links{font-size:13px}.hero{padding:50px 0}.hero h1{font-size:48px}.section-head{display:block}}
</style></head><body><nav class="nav"><a class="brand" href="/"><span class="brand-mark"></span><span>NEXAUREN</span></a><div class="links"><a href="/">Início</a><a href="/tools">Ferramentas</a><a href="/register">Criar conta</a><a href="/login">Entrar</a><a href="/account">Minha conta</a><a href="/dashboard">Admin</a></div></nav>${body}<footer class="wrap footer"><strong>Nexauren</strong> — ferramentas digitais simples, rápidas e profissionais.</footer></body></html>`, { headers: { "Content-Type": "text/html; charset=UTF-8" } });
}

let requestUrl = "https://nexaurenstory.com/";

function redirect(url, extraHeaders = {}) { return new Response(null, { status: 302, headers: { Location: url, ...extraHeaders } }); }

async function hashPassword(password, salt) { const data = new TextEncoder().encode(password + salt); const digest = await crypto.subtle.digest("SHA-256", data); return [...new Uint8Array(digest)].map((x) => x.toString(16).padStart(2, "0")).join(""); }
function randomToken() { const bytes = new Uint8Array(32); crypto.getRandomValues(bytes); return [...bytes].map((x) => x.toString(16).padStart(2, "0")).join(""); }

async function userFromRequest(request, env) { const cookie = request.headers.get("Cookie") || ""; const match = cookie.match(new RegExp(`${COOKIE}=([^;]+)`)); if (!match) return null; return env.DB.prepare("SELECT u.id,u.email,u.name,u.role FROM sessions s JOIN users u ON u.id=s.user_id WHERE s.token=? AND s.expires_at>? LIMIT 1").bind(match[1], Date.now()).first(); }

async function home(env) {
  const categories = await env.DB.prepare(`SELECT c.slug,c.name,c.description,c.icon,COUNT(t.slug) AS count FROM categories c LEFT JOIN tools t ON t.category=c.slug AND t.published=1 GROUP BY c.slug,c.name,c.description,c.icon ORDER BY c.name`).all();
  const featured = await env.DB.prepare("SELECT slug,name,description,category FROM tools WHERE published=1 ORDER BY created_at DESC LIMIT 12").all();
  return page(`<main class="wrap"><section class="hero"><span class="eyebrow">✦ Nexauren Tools</span><h1>Ferramentas digitais <span class="gradient">feitas para simplificar.</span></h1><p class="lead">Um espaço moderno para encontrar ferramentas online rápidas, organizadas e fáceis de usar. Explore por categoria e comece em poucos segundos.</p><div class="actions"><a class="btn" href="/tools">Explorar ferramentas →</a><a class="btn secondary" href="/register">Criar conta</a></div></section><div class="section-head"><div><h2>Categorias</h2><p class="muted">Encontre rapidamente o que precisa.</p></div></div><div class="grid">${categories.results.map((c) => `<a class="card" href="/tools?category=${encodeURIComponent(c.slug)}"><span class="pill">${escapeHtml(c.icon || c.slug)}</span><h3>${escapeHtml(c.name)}</h3><p class="muted">${escapeHtml(c.description || "Ferramentas Nexauren")} · ${c.count} disponível(eis)</p></a>`).join("")}</div><div class="section-head" style="margin-top:56px"><div><h2>Ferramentas recentes</h2><p class="muted">Novidades publicadas no Nexauren.</p></div></div><div class="grid">${featured.results.map((t) => `<a class="card" href="/tool/${encodeURIComponent(t.slug)}"><span class="pill">${escapeHtml(t.category)}</span><h3>${escapeHtml(t.name)}</h3><p class="muted">${escapeHtml(t.description || "Ferramenta online Nexauren")}</p></a>`).join("") || `<div class="card"><p class="muted">As primeiras ferramentas estão a ser preparadas.</p></div>`}</div></main>`, "Nexauren — Ferramentas Online", "Nexauren oferece ferramentas online rápidas, modernas e fáceis de usar para produtividade, áudio, imagem, PDF, texto e muito mais.");
}

async function tools(request, env) {
  const url = new URL(request.url); const category = url.searchParams.get("category");
  const result = category ? await env.DB.prepare("SELECT slug,name,description,category FROM tools WHERE published=1 AND category=? ORDER BY name").bind(category).all() : await env.DB.prepare("SELECT slug,name,description,category FROM tools WHERE published=1 ORDER BY created_at DESC").all();
  return page(`<main class="wrap"><div class="section-head"><div><span class="eyebrow">Nexauren Tools</span><h1>Ferramentas</h1><p class="muted">${category ? `Categoria: ${escapeHtml(category)}` : "Todas as ferramentas disponíveis."}</p></div></div><div class="grid">${result.results.map((t) => `<a class="card" href="/tool/${encodeURIComponent(t.slug)}"><span class="pill">${escapeHtml(t.category)}</span><h3>${escapeHtml(t.name)}</h3><p class="muted">${escapeHtml(t.description || "Ferramenta online Nexauren")}</p><span class="gradient" style="font-weight:800">Usar ferramenta →</span></a>`).join("") || `<div class="card"><p class="muted">Nenhuma ferramenta publicada nesta categoria.</p></div>`}</div></main>`, "Ferramentas Online | Nexauren", "Explore as ferramentas online do Nexauren por categoria.");
}

function validToolPath(value) { return /^tools\/(audio|image|pdf|text|productivity|business|marketplace)\/[a-z0-9-]+$/.test(value); }
async function toolPage(slug, env, request) { const tool = await env.DB.prepare("SELECT slug,name,description,category,tool_path,html_file,js_file,css_file FROM tools WHERE slug=? AND published=1 LIMIT 1").bind(slug).first(); if (!tool) return new Response("Ferramenta não encontrada", { status: 404 }); if (!validToolPath(tool.tool_path)) return new Response("Caminho da ferramenta inválido", { status: 500 }); if (!env.ASSETS) return new Response("Assets não configurados", { status: 500 }); const assetUrl = new URL(`/${tool.tool_path}/${tool.html_file || "index.html"}`, request.url); const assetResponse = await env.ASSETS.fetch(new Request(assetUrl, request)); if (!assetResponse.ok) return new Response("Arquivos da ferramenta ainda não foram publicados", { status: 404 }); return assetResponse; }

function authForm(type) { const register = type === "register"; return page(`<main class="wrap"><form class="card form" method="post"><span class="eyebrow">Nexauren Account</span><h1>${register ? "Criar conta" : "Bem-vindo de volta"}</h1><p class="muted">${register ? "Crie a sua conta para aceder à experiência Nexauren." : "Entre na sua conta Nexauren."}</p>${register ? `<div class="field"><label>Nome</label><input name="name" required maxlength="80" autocomplete="name"></div>` : ""}<div class="field"><label>Email</label><input type="email" name="email" required maxlength="160" autocomplete="email"></div><div class="field"><label>Senha</label><input type="password" name="password" required minlength="8" autocomplete="${register ? "new-password" : "current-password"}"></div><button class="btn" type="submit">${register ? "Criar minha conta" : "Entrar na conta"}</button><p class="muted">${register ? `Já tem conta? <a class="gradient" style="font-weight:800" href="/login">Entrar</a>` : `Ainda não tem conta? <a class="gradient" style="font-weight:800" href="/register">Criar conta</a>`}</p></form></main>`, register ? "Criar conta | Nexauren" : "Login | Nexauren", "Aceda à sua conta Nexauren."); }

async function auth(request, env, type) { if (request.method === "GET") return authForm(type); const form = await request.formData(); const email = String(form.get("email") || "").trim().toLowerCase(); const password = String(form.get("password") || ""); if (!email || password.length < 8) return new Response("Email e senha válidos são obrigatórios", { status: 400 }); if (type === "register") { const count = await env.DB.prepare("SELECT COUNT(*) AS count FROM users").first(); const exists = await env.DB.prepare("SELECT id FROM users WHERE email=? LIMIT 1").bind(email).first(); if (exists) return redirect("/login"); const id = randomToken(); const salt = randomToken().slice(0, 16); const hash = await hashPassword(password, salt); const role = Number(count.count) === 0 ? "admin" : "user"; const name = String(form.get("name") || "Utilizador").trim(); await env.DB.prepare("INSERT INTO users(id,email,name,password_hash,password_salt,role,created_at) VALUES(?,?,?,?,?,?,?)").bind(id,email,name,hash,salt,role,Date.now()).run(); const token = randomToken(); await env.DB.prepare("INSERT INTO sessions(token,user_id,expires_at) VALUES(?,?,?)").bind(token,id,Date.now() + SESSION_DAYS * 86400000).run(); return redirect("/account", { "Set-Cookie": `${COOKIE}=${token}; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=${SESSION_DAYS * 86400}` }); } const user = await env.DB.prepare("SELECT * FROM users WHERE email=? LIMIT 1").bind(email).first(); if (!user || await hashPassword(password, user.password_salt) !== user.password_hash) return new Response("Email ou senha inválidos", { status: 401 }); const token = randomToken(); await env.DB.prepare("INSERT INTO sessions(token,user_id,expires_at) VALUES(?,?,?)").bind(token,user.id,Date.now() + SESSION_DAYS * 86400000).run(); return redirect("/account", { "Set-Cookie": `${COOKIE}=${token}; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=${SESSION_DAYS * 86400}` }); }

async function account(request, env) { const user = await userFromRequest(request, env); if (!user) return redirect("/login"); return page(`<main class="wrap"><div class="card"><span class="eyebrow">Minha conta</span><h1>Olá, ${escapeHtml(user.name)}.</h1><p class="muted">${escapeHtml(user.email)}</p><p><span class="pill">${escapeHtml(user.role)}</span></p><div class="actions"><a class="btn" href="/tools">Explorar ferramentas</a><form method="post" action="/logout"><button class="btn secondary">Sair</button></form></div></div></main>`, "Minha conta | Nexauren", "Área da conta Nexauren."); }

async function logout(request, env) { const cookie = request.headers.get("Cookie") || ""; const match = cookie.match(new RegExp(`${COOKIE}=([^;]+)`)); if (match) await env.DB.prepare("DELETE FROM sessions WHERE token=?").bind(match[1]).run(); return redirect("/", { "Set-Cookie": `${COOKIE}=; Path=/; Max-Age=0; HttpOnly; Secure; SameSite=Lax` }); }

async function dashboard(request, env) { const user = await userFromRequest(request, env); if (!user || user.role !== "admin") return redirect(user ? "/account" : "/login"); const users = await env.DB.prepare("SELECT COUNT(*) AS count FROM users").first(); const toolsCount = await env.DB.prepare("SELECT COUNT(*) AS count FROM tools WHERE published=1").first(); return page(`<main class="wrap"><span class="eyebrow">Admin</span><h1>Centro de controlo</h1><p class="lead">Área administrativa do Nexauren para gestão de utilizadores, notificações, blog, editor e controlo da plataforma.</p><div class="stats"><div class="stat"><span class="muted">Utilizadores</span><strong>${users.count}</strong></div><div class="stat"><span class="muted">Ferramentas publicadas</span><strong>${toolsCount.count}</strong></div></div><div class="grid"><div class="card"><h3>👥 Utilizadores</h3><p class="muted">Gestão e controlo das contas da plataforma.</p></div><div class="card"><h3>📢 Notificações</h3><p class="muted">Área reservada para comunicados aos utilizadores.</p></div><div class="card"><h3>📝 Blog</h3><p class="muted">Publicação e gestão do conteúdo editorial.</p></div><div class="card"><h3>✏️ Editor</h3><p class="muted">Ferramentas de edição e controlo administrativo.</p></div></div><div class="notice">As ferramentas são desenvolvidas e publicadas pela Nexauren. O painel administrativo não é usado para criar ferramentas.</div></main>`, "Admin | Nexauren", "Centro administrativo do Nexauren."); }

export default { async fetch(request, env) { try { requestUrl = request.url; const url = new URL(request.url); const path = url.pathname.replace(/\/+$/, "") || "/"; if (request.method === "OPTIONS") return new Response(null, { headers: { "Access-Control-Allow-Origin": "*" } }); if (path === "/") return home(env); if (path === "/tools") return tools(request, env); if (path.startsWith("/tool/")) return toolPage(decodeURIComponent(path.slice(6)), env, request); if (path === "/register") return auth(request, env, "register"); if (path === "/login") return auth(request, env, "login"); if (path === "/account") return account(request, env); if (path === "/logout" && request.method === "POST") return logout(request, env); if (path === "/dashboard") return dashboard(request, env); return new Response("Not found", { status: 404 }); } catch (error) { return new Response(`Nexauren error: ${error.message}`, { status: 500 }); } } };
