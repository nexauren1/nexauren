const BLOG_TYPES=["article","guide","news","breaking_news","review","comparison","opinion","announcement"];
const BLOG_STATUSES=["draft","published","scheduled"];
function json(data,status=200){return new Response(JSON.stringify(data),{status,headers:{"Content-Type":"application/json; charset=UTF-8","Cache-Control":"no-store"}});}
function slugify(value){return String(value||"").normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase().trim().replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"").slice(0,180);}
function readingTime(content){const text=String(content||"").replace(/<[^>]*>/g," ").replace(/\s+/g," ").trim();return text?Math.max(1,Math.ceil(text.split(" ").length/200)):0;}
function publicArticleUrl(req,slug){return `${new URL(req.url).origin}/blog/${encodeURIComponent(slug)}/`;}
function publicImageUrl(req,slug){return `${new URL(req.url).origin}/api/blog/image/${encodeURIComponent(slug)}`;}
async function currentUser(req,env){const cookie=req.headers.get("Cookie")||"";const match=cookie.match(/nexauren_session=([^;]+)/);if(!match)return null;return env.DB.prepare("SELECT u.id,u.email,u.name,u.role FROM sessions s JOIN users u ON u.id=s.user_id WHERE s.token=? AND s.expires_at>? LIMIT 1").bind(match[1],Date.now()).first();}
async function requireAdmin(req,env){const user=await currentUser(req,env);if(!user)return{response:json({error:"Please sign in."},401)};if(String(user.role).toLowerCase()!=="admin")return{response:json({error:"Admin access required."},403)};return{user};}
const PUBLISHED_FILTER="(p.published_at IS NULL OR datetime(replace(replace(p.published_at,'T',' '),'Z',''))<=CURRENT_TIMESTAMP)";
async function publicPosts(req,env){const url=new URL(req.url),slug=url.searchParams.get("slug"),category=url.searchParams.get("category"),limit=Math.min(50,Math.max(1,Number(url.searchParams.get("limit")||12)));if(slug){const post=await env.BLOG_DB.prepare("SELECT p.*,c.name AS category_name,c.slug AS category_slug,a.name AS author_name,a.slug AS author_slug FROM posts p LEFT JOIN categories c ON c.id=p.category_id LEFT JOIN authors a ON a.id=p.author_id WHERE p.slug=? AND p.status='published' AND "+PUBLISHED_FILTER+" LIMIT 1").bind(slug).first();if(!post)return json({error:"Post not found."},404);post.article_url=publicArticleUrl(req,post.slug);post.preview_image_url=post.cover_image?publicImageUrl(req,post.slug):null;return json({ok:true,post});}let query="SELECT p.id,p.title,p.slug,p.excerpt,p.cover_image,p.cover_image_alt,p.category_id,p.author_id,p.type,p.reading_time,p.views,p.published_at,c.name AS category_name,c.slug AS category_slug,a.name AS author_name FROM posts p LEFT JOIN categories c ON c.id=p.category_id LEFT JOIN authors a ON a.id=p.author_id WHERE p.status='published' AND "+PUBLISHED_FILTER+" ";const bindings=[];if(category){query+="AND c.slug=? ";bindings.push(category);}query+="ORDER BY COALESCE(p.published_at,p.created_at) DESC LIMIT ?";bindings.push(limit);const result=await env.BLOG_DB.prepare(query).bind(...bindings).all();const posts=(result.results||[]).map(post=>({...post,article_url:publicArticleUrl(req,post.slug),preview_image_url:post.cover_image?publicImageUrl(req,post.slug):null}));return json({ok:true,posts});}
async function publicPostImage(req,env,slug){const post=await env.BLOG_DB.prepare("SELECT cover_image FROM posts WHERE slug=? AND status='published' AND "+PUBLISHED_FILTER+" LIMIT 1").bind(slug).first();const source=String(post?.cover_image||"").trim();if(!source)return new Response("Image not found.",{status:404});let imageUrl;try{imageUrl=new URL(source);}catch(_){return new Response("Invalid image URL.",{status:400});}if(!["http:","https:"].includes(imageUrl.protocol))return new Response("Unsupported image URL.",{status:400});try{const response=await fetch(imageUrl.toString(),{headers:{Accept:"image/avif,image/webp,image/png,image/jpeg,image/*"}});if(!response.ok)return new Response("Image unavailable.",{status:404});const type=response.headers.get("Content-Type")||"";if(!type.toLowerCase().startsWith("image/"))return new Response("Resource is not an image.",{status:415});const headers=new Headers();headers.set("Content-Type",type.split(";")[0]);headers.set("Cache-Control","public, max-age=86400, s-maxage=604800, stale-while-revalidate=86400");headers.set("X-Content-Type-Options","nosniff");return new Response(response.body,{status:200,headers});}catch(_){return new Response("Could not load image.",{status:502});}}
function uaInfo(ua){const value=String(ua||"").toLowerCase();const device=/tablet|ipad/.test(value)?"tablet":/mobile|android|iphone|ipod/.test(value)?"mobile":"desktop";const browser=/edg\//.test(value)?"Edge":/chrome\//.test(value)&&!/edg\//.test(value)?"Chrome":/firefox\//.test(value)?"Firefox":/safari\//.test(value)&&!/chrome\//.test(value)?"Safari":/opera|opr\//.test(value)?"Opera":"Other";return{device,browser};}
function referrerHost(value){try{return new URL(value).hostname.replace(/^www\./,"").slice(0,180)||"Direct";}catch(_){return value?"Other":"Direct";}}
async function recordAnalytics(req,env){if(req.method!=="POST")return null;const body=await req.json().catch(()=>null);if(!body)return json({error:"Invalid JSON."},400);const slug=slugify(body.slug),eventType=["view","engagement","click"].includes(body.event)?body.event:null;if(!slug||!eventType)return json({error:"slug and a valid event are required."},400);const post=await env.BLOG_DB.prepare("SELECT id FROM posts WHERE slug=? AND status='published' AND "+PUBLISHED_FILTER+" LIMIT 1").bind(slug).first();if(!post)return json({error:"Post not found."},404);const ua=uaInfo(req.headers.get("User-Agent"));const duration=Math.min(3600000,Math.max(0,Number(body.duration_ms)||0));const country=String(req.headers.get("CF-IPCountry")||"").slice(0,8)||null;const referrer=referrerHost(req.headers.get("Referer")||"");await env.BLOG_DB.prepare("INSERT INTO blog_analytics_events(post_id,event_type,duration_ms,country,referrer_host,device_type,browser) VALUES(?,?,?,?,?,?,?)").bind(post.id,eventType,duration,country,referrer,ua.device,ua.browser).run();return json({ok:true});}
async function adminAnalytics(req,env){const auth=await requireAdmin(req,env);if(auth.response)return auth.response;const url=new URL(req.url);const range=url.searchParams.get("range")||"7";let from=url.searchParams.get("from"),to=url.searchParams.get("to");const now=new Date();if(!from||!to){const days=range==="28"?28:7;const start=new Date(now);start.setUTCDate(start.getUTCDate()-(days-1));from=start.toISOString().slice(0,10);to=now.toISOString().slice(0,10);}if(!/^\d{4}-\d{2}-\d{2}$/.test(from)||!/^\d{4}-\d{2}-\d{2}$/.test(to))return json({error:"Invalid date range."},400);const slug=String(url.searchParams.get("slug")||"").trim();const filter=slug?" AND p.slug=?":"";const bind=[`${from} 00:00:00`,`${to} 23:59:59`];if(slug)bind.push(slug);const base="FROM blog_analytics_events e JOIN posts p ON p.id=e.post_id WHERE e.occurred_at BETWEEN ? AND ?"+filter;const summary=await env.BLOG_DB.prepare("SELECT COUNT(CASE WHEN e.event_type='view' THEN 1 END) AS views,COUNT(CASE WHEN e.event_type='click' THEN 1 END) AS clicks,COUNT(CASE WHEN e.event_type='engagement' THEN 1 END) AS engaged_reads,COALESCE(AVG(CASE WHEN e.event_type='engagement' AND e.duration_ms>0 THEN e.duration_ms END),0) AS avg_read_ms "+base).bind(...bind).first();const daily=await env.BLOG_DB.prepare("SELECT substr(e.occurred_at,1,10) AS day,COUNT(CASE WHEN e.event_type='view' THEN 1 END) AS views,COUNT(CASE WHEN e.event_type='click' THEN 1 END) AS clicks,COUNT(CASE WHEN e.event_type='engagement' THEN 1 END) AS reads "+base+" GROUP BY day ORDER BY day").bind(...bind).all();const topPosts=await env.BLOG_DB.prepare("SELECT p.slug,p.title,COUNT(CASE WHEN e.event_type='view' THEN 1 END) AS views,COUNT(CASE WHEN e.event_type='click' THEN 1 END) AS clicks,COALESCE(AVG(CASE WHEN e.event_type='engagement' AND e.duration_ms>0 THEN e.duration_ms END),0) AS avg_read_ms "+base+" GROUP BY p.id ORDER BY views DESC LIMIT 10").bind(...bind).all();const sources=await env.BLOG_DB.prepare("SELECT COALESCE(NULLIF(e.referrer_host,''),'Direct') AS source,COUNT(*) AS visits "+base+" AND e.event_type='view' GROUP BY source ORDER BY visits DESC LIMIT 12").bind(...bind).all();const countries=await env.BLOG_DB.prepare("SELECT COALESCE(NULLIF(e.country,''),'Unknown') AS country,COUNT(*) AS visits "+base+" AND e.event_type='view' GROUP BY country ORDER BY visits DESC LIMIT 12").bind(...bind).all();const devices=await env.BLOG_DB.prepare("SELECT COALESCE(NULLIF(e.device_type,''),'unknown') AS device,COUNT(*) AS visits "+base+" AND e.event_type='view' GROUP BY device ORDER BY visits DESC").bind(...bind).all();const browsers=await env.BLOG_DB.prepare("SELECT COALESCE(NULLIF(e.browser,''),'Other') AS browser,COUNT(*) AS visits "+base+" AND e.event_type='view' GROUP BY browser ORDER BY visits DESC").bind(...bind).all();return json({ok:true,range:{from,to},summary:{views:Number(summary?.views||0),clicks:Number(summary?.clicks||0),engaged_reads:Number(summary?.engaged_reads||0),avg_read_ms:Number(summary?.avg_read_ms||0)},daily:daily.results||[],top_posts:topPosts.results||[],sources:sources.results||[],countries:countries.results||[],devices:devices.results||[],browsers:browsers.results||[]});}
async function adminCategories(req,env){const result=await env.BLOG_DB.prepare("SELECT * FROM categories ORDER BY name ASC").all();return json({ok:true,categories:result.results||[]});}
async function adminAuthors(req,env){const result=await env.BLOG_DB.prepare("SELECT * FROM authors ORDER BY name ASC").all();return json({ok:true,authors:result.results||[]});}
async function adminPosts(req,env){const id=new URL(req.url).searchParams.get("id");if(id){const post=await env.BLOG_DB.prepare("SELECT p.*,c.name AS category_name,a.name AS author_name FROM posts p LEFT JOIN categories c ON c.id=p.category_id LEFT JOIN authors a ON a.id=p.author_id WHERE p.id=? LIMIT 1").bind(id).first();if(!post)return json({error:"Post not found."},404);return json({ok:true,post});}const result=await env.BLOG_DB.prepare("SELECT p.id,p.title,p.slug,p.excerpt,p.type,p.status,p.cover_image,p.reading_time,p.views,p.published_at,p.created_at,p.updated_at,c.name AS category_name,a.name AS author_name FROM posts p LEFT JOIN categories c ON c.id=p.category_id LEFT JOIN authors a ON a.id=p.author_id ORDER BY p.updated_at DESC").all();return json({ok:true,posts:result.results||[]});}
async function createPost(req,env){const body=await req.json().catch(()=>null);if(!body)return json({error:"Invalid JSON."},400);const title=String(body.title||"").trim(),content=String(body.content||"").trim();if(!title||!content)return json({error:"Title and content are required."},400);const type=BLOG_TYPES.includes(body.type)?body.type:"article",status=BLOG_STATUSES.includes(body.status)?body.status:"draft",slug=slugify(body.slug||title);if(!slug)return json({error:"A valid slug is required."},400);if(await env.BLOG_DB.prepare("SELECT id FROM posts WHERE slug=? LIMIT 1").bind(slug).first())return json({error:"That slug is already in use."},409);const result=await env.BLOG_DB.prepare("INSERT INTO posts(title,slug,excerpt,content,cover_image,cover_image_alt,category_id,author_id,status,type,seo_title,seo_description,seo_keywords,canonical_url,reading_time,published_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,CURRENT_TIMESTAMP)").bind(title,slug,String(body.excerpt||"").trim()||null,content,String(body.cover_image||"").trim()||null,String(body.cover_image_alt||"").trim()||null,body.category_id?Number(body.category_id):null,body.author_id?Number(body.author_id):null,status,type,String(body.seo_title||"").trim()||null,String(body.seo_description||"").trim()||null,String(body.seo_keywords||"").trim()||null,String(body.canonical_url||"").trim()||null,readingTime(content),status==="published"?(body.published_at||new Date().toISOString()):null).run();return json({ok:true,id:result.meta?.last_row_id||null,slug,status,article_url:status==="published"?publicArticleUrl(req,slug):null,preview_image_url:status==="published"&&body.cover_image?publicImageUrl(req,slug):null},201);}
async function updatePost(req,env,id){const body=await req.json().catch(()=>null);if(!body)return json({error:"Invalid JSON."},400);const current=await env.BLOG_DB.prepare("SELECT * FROM posts WHERE id=? LIMIT 1").bind(id).first();if(!current)return json({error:"Post not found."},404);const title=String(body.title??current.title).trim(),content=String(body.content??current.content).trim(),type=BLOG_TYPES.includes(body.type)?body.type:current.type||"article",status=BLOG_STATUSES.includes(body.status)?body.status:current.status,slug=slugify(body.slug||title);if(await env.BLOG_DB.prepare("SELECT id FROM posts WHERE slug=? AND id<>? LIMIT 1").bind(slug,id).first())return json({error:"That slug is already in use."},409);const publishedAt=status==="published"?(body.published_at||current.published_at||new Date().toISOString()):null,coverImage=String(body.cover_image??current.cover_image??"").trim()||null;await env.BLOG_DB.prepare("UPDATE posts SET title=?,slug=?,excerpt=?,content=?,cover_image=?,cover_image_alt=?,category_id=?,author_id=?,status=?,type=?,seo_title=?,seo_description=?,seo_keywords=?,canonical_url=?,reading_time=?,published_at=?,updated_at=CURRENT_TIMESTAMP WHERE id=?").bind(title,slug,String(body.excerpt??current.excerpt??"").trim()||null,content,coverImage,String(body.cover_image_alt??current.cover_image_alt??"").trim()||null,body.category_id===undefined?current.category_id:(body.category_id?Number(body.category_id):null),body.author_id===undefined?current.author_id:(body.author_id?Number(body.author_id):null),status,type,String(body.seo_title??current.seo_title??"").trim()||null,String(body.seo_description??current.seo_description??"").trim()||null,String(body.seo_keywords??current.seo_keywords??"").trim()||null,String(body.canonical_url??current.canonical_url??"").trim()||null,readingTime(content),publishedAt,id).run();return json({ok:true,id:Number(id),slug,status,article_url:status==="published"?publicArticleUrl(req,slug):null,preview_image_url:status==="published"&&coverImage?publicImageUrl(req,slug):null});}
async function deletePost(req,env,id){const result=await env.BLOG_DB.prepare("DELETE FROM posts WHERE id=?").bind(id).run();if(!result.meta?.changes)return json({error:"Post not found."},404);return json({ok:true});}
export default{async fetch(req,env){const path=new URL(req.url).pathname,method=req.method;if(!env.BLOG_DB)return json({error:"Blog database is not configured."},500);const imageMatch=path.match(/^\/api\/blog\/image\/([^/]+)\/?$/);if(imageMatch&&method==="GET")return publicPostImage(req,env,decodeURIComponent(imageMatch[1]));if(path==="/api/blog/analytics"&&method==="POST")return recordAnalytics(req,env);if(path==="/api/blog/admin/analytics"&&method==="GET")return adminAnalytics(req,env);if(path==="/api/blog/posts"&&method==="GET")return publicPosts(req,env);if(path==="/api/blog/admin/categories"&&method==="GET"){const auth=await requireAdmin(req,env);if(auth.response)return auth.response;return adminCategories(req,env);}if(path==="/api/blog/admin/authors"&&method==="GET"){const auth=await requireAdmin(req,env);if(auth.response)return auth.response;return adminAuthors(req,env);}if(path==="/api/blog/admin/posts"&&method==="GET"){const auth=await requireAdmin(req,env);if(auth.response)return auth.response;return adminPosts(req,env);}if(path==="/api/blog/admin/posts"&&method==="POST"){const auth=await requireAdmin(req,env);if(auth.response)return auth.response;return createPost(req,env);}const match=path.match(/^\/api\/blog\/admin\/posts\/(\d+)$/);if(match&&method==="PUT"){const auth=await requireAdmin(req,env);if(auth.response)return auth.response;return updatePost(req,env,match[1]);}if(match&&method==="DELETE"){const auth=await requireAdmin(req,env);if(auth.response)return auth.response;return deletePost(req,env,match[1]);}return null;}};
