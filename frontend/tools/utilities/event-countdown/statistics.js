const select=document.querySelector("#eventSelect"),message=document.querySelector("#message"),viewsEl=document.querySelector("#views"),clicksEl=document.querySelector("#clicks"),ctrEl=document.querySelector("#ctr"),bestDayEl=document.querySelector("#bestDay"),bestDayValue=document.querySelector("#bestDayValue"),chart=document.querySelector("#chart"),chartTotal=document.querySelector("#chartTotal"),rows=document.querySelector("#rows"),topEvents=document.querySelector("#topEvents"),viewBar=document.querySelector("#viewBar"),clickBar=document.querySelector("#clickBar"),viewBarValue=document.querySelector("#viewBarValue"),clickBarValue=document.querySelector("#clickBarValue"),insight=document.querySelector("#insight");let events=[],stats={},range=30;
const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));const api=async url=>{const r=await fetch(url,{credentials:"same-origin",headers:{Accept:"application/json"}}),d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||"Request failed.");return d};const num=v=>Number(v||0).toLocaleString();
function filteredDays(){return(stats[select.value]?.days||[]).slice(0,range).reverse()}
function renderChart(days){const w=900,h=300,p={l:42,r:18,t:18,b:42},vals=days.map(x=>Number(x.views||0)),max=Math.max(1,...vals),iw=w-p.l-p.r,ih=h-p.t-p.b,x=i=>p.l+(days.length<=1?iw/2:i/(days.length-1)*iw),y=v=>p.t+(1-v/max)*ih,points=vals.map((v,i)=>`${x(i)},${y(v)}`),area=points.length?`${points.join(" L ")} L ${x(vals.length-1)},${h-p.b} L ${x(0)},${h-p.b} Z`:"";let grid="",labels="";for(let i=0;i<4;i++){const yy=p.t+i*ih/3;grid+=`<line class="chart-grid" x1="${p.l}" y1="${yy}" x2="${w-p.r}" y2="${yy}"/>`}const step=Math.max(1,Math.ceil(days.length/6));days.forEach((d,i)=>{if(i%step===0||i===days.length-1)labels+=`<text class="chart-label" x="${x(i)}" y="${h-14}" text-anchor="middle">${esc(d.day.slice(5))}</text>`});const dots=vals.map((v,i)=>`<circle class="chart-dot" cx="${x(i)}" cy="${y(v)}" r="4"><title>${esc(days[i].day)} · ${num(v)} views</title></circle>`).join("");chart.innerHTML=`<defs><linearGradient id="area" x1="0" x2="0" y1="0" y2="1"><stop offset="0" stop-color="#7fa8ff" stop-opacity=".24"/><stop offset="1" stop-color="#7fa8ff" stop-opacity="0"/></linearGradient></defs>${grid}${area?`<path class="chart-area" d="M ${area}"/>`:""}${points.length>1?`<polyline class="chart-line" points="${points.join(" ")}"/>`:""}${dots}${labels}`}
function render(){const days=filteredDays(),value=stats[select.value]||{views:0,link_clicks:0,days:[]},views=days.reduce((a,d)=>a+Number(d.views||0),0),clicks=days.reduce((a,d)=>a+Number(d.link_clicks||0),0),ctr=views?clicks/views*100:0;viewsEl.textContent=num(views);clicksEl.textContent=num(clicks);ctrEl.textContent=`${ctr.toFixed(1)}%`;chartTotal.textContent=`${num(views)} views`;const best=days.reduce((a,d)=>Number(d.views||0)>Number(a?.views||0)?d:a,null);bestDayEl.textContent=best?best.day.slice(5):"—";bestDayValue.textContent=best?`${num(best.views)} views`:"No activity yet";const max=Math.max(1,views);viewBar.style.width=`${Math.min(100,views/max*100)}%`;clickBar.style.width=`${Math.min(100,clicks/max*100)}%`;viewBarValue.textContent=num(views);clickBarValue.textContent=num(clicks);if(!views)insight.textContent="No traffic in this period yet. Share your public countdown to start collecting analytics.";else if(ctr>=10)insight.textContent=`Strong conversion: ${ctr.toFixed(1)}% of views became event-link clicks.`;else if(ctr>0)insight.textContent=`Your event link received ${num(clicks)} clicks from ${num(views)} views. Keep the CTA clear and easy to find.`;else insight.textContent="People are viewing your countdown, but nobody has clicked the event link in this period.";rows.innerHTML=days.slice().reverse().map(d=>{const v=Number(d.views||0),c=Number(d.link_clicks||0);return`<tr><td>${esc(d.day)}</td><td>${num(v)}</td><td>${num(c)}</td><td>${v?(c/v*100).toFixed(1):"0.0"}%</td></tr>`}).join("")||`<tr><td colspan="4" class="empty-stats">No activity yet</td></tr>`;renderChart(days)}
function renderTop(){const ranked=events.map(e=>({e,s:stats[e.id]||{}})).sort((a,b)=>Number(b.s.views||0)-Number(a.s.views||0)).slice(0,8);topEvents.innerHTML=ranked.map((x,i)=>`<div class="top-row"><span class="rank">${i+1}</span><div class="top-name">${esc(x.e.name)}<small>${esc(x.e.theme||"Nexauren Classic")}</small></div><span class="top-num">${num(x.s.views)} views</span><span class="top-num">${num(x.s.link_clicks)} clicks</span></div>`).join("")}
async function load(){try{const data=await api("/api/tools/event-countdown/events");events=data.events||[];if(!events.length){select.innerHTML="<option>No events yet</option>";message.textContent="Create an event first to see statistics.";return}select.innerHTML=events.map(e=>`<option value="${esc(e.id)}">${esc(e.name)}</option>`).join("");const results=await Promise.all(events.map(async e=>[e.id,await api(`/api/tools/event-countdown/events/${encodeURIComponent(e.id)}/stats`)]));stats=Object.fromEntries(results);const wanted=new URLSearchParams(location.search).get("event");if(wanted&&events.some(e=>e.id===wanted))select.value=wanted;renderTop();render()}catch(e){if(e.message.includes("sign in"))location.href="/login?next=/tools/utilities/event-countdown/statistics.html";else message.textContent=e.message}}
select.addEventListener("change",render);document.querySelectorAll("[data-range]").forEach(b=>b.addEventListener("click",()=>{range=Number(b.dataset.range);document.querySelectorAll("[data-range]").forEach(x=>x.classList.toggle("active",x===b));render()}));load();