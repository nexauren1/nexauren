name: Nexauren analytics and Free tool ads

on:
  push:
    branches: [main]
    paths:
      - "frontend/**/*.html"
      - "frontend/js/G4A.js"
      - "frontend/js/nexauren-ads.js"
      - ".github/workflows/nexauren-monetization.yml"

permissions:
  contents: write

concurrency:
  group: nexauren-monetization-main
  cancel-in-progress: false

jobs:
  inject:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Inject G4A and ads only into eligible pages
        run: |
          python - <<'PY'
          from pathlib import Path

          root = Path("frontend")
          g4a = '<script src="/js/G4A.js" defer></script>'
          old_analytics = '<script src="/js/nexauren-analytics.js" defer></script>'
          ads = '<script src="/js/nexauren-ads.js" defer></script>'
          changed = []

          for path in sorted(root.rglob("*.html")):
              text = path.read_text(encoding="utf-8")
              original = text

              text = text.replace(old_analytics, "")

              if g4a not in text and "</head>" in text:
                  text = text.replace(
                      "</head>",
                      f"  {g4a}\n</head>",
                      1,
                  )

              relative = path.as_posix()

              is_individual_tool = (
                  relative.startswith("frontend/tools/")
                  and path.name == "index.html"
                  and relative != "frontend/tools/index.html"
              )

              is_blog_article_shell = (
                  relative == "frontend/blog/article/index.html"
              )

              if (
                  (is_individual_tool or is_blog_article_shell)
                  and ads not in text
                  and "</head>" in text
              ):
                  text = text.replace(
                      "</head>",
                      f"  {ads}\n</head>",
                      1,
                  )

              if text != original:
                  path.write_text(text, encoding="utf-8")
                  changed.append(relative)

          print(f"Updated {len(changed)} HTML pages")
          for item in changed:
              print(item)
          PY

      - name: Commit and push without race failures
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add frontend

          if git diff --cached --quiet; then
            exit 0
          fi

          git commit -m "Apply ads automatically to blog article pages [skip ci]"
          git fetch origin main
          git rebase origin/main
          git push origin HEAD:main
